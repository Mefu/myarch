# program launcher
super + d
  exec bash -c "rofi -show combi -combi-modi 'run,drun,ssh' -modi combi -font 'Noto Sans 14' -theme solarized_alternate -drun-show-actions"

# window switcher
super + Tab
  exec bash -c "rofi -modi 'window' -show window -font 'Noto Sans 14' -theme solarized_alternate"

